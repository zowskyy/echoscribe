[project]
name = "echoscribe-linux"
version = "0.1.0"
description = "Linux desktop companion for EchoScribe push-to-talk transcription and browser summaries."
requires-python = ">=3.9"
dependencies = [
]

[tool.setuptools]
packages = ["echoscribe"]
